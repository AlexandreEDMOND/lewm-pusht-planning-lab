# Copy to config/local.env and adapt this path on the RTX 3090 machine.
# The directory is intentionally ignored by Git: it can hold datasets and checkpoints.
STABLEWM_HOME=.local/stablewm

# Set to offline until experiment tracking is configured.
WANDB_MODE=offline
